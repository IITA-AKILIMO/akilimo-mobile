<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Your scrollable or main content goes here -->
    <ScrollView
        android:id="@+id/content_scroll"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginBottom="72dp"> <!-- leaves space for button -->

        <!-- Replace with your actual content layout -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">
            <!-- Add your content here -->
        </LinearLayout>
    </ScrollView>

    <!-- Bottom-fixed button -->
    <LinearLayout
        android:id="@+id/lyt_button"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:background="?android:attr/windowBackground"
        android:elevation="4dp"
        android:gravity="center"
        android:orientation="horizontal"
        android:padding="16dp">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnGetRecommendation"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="match_parent"
            android:layout_height="@dimen/dimen_58"
            android:fontFamily="@font/ubuntu_bold"
            android:text="@string/lbl_get_recommendations"
            android:textAllCaps="false"
            android:textStyle="bold"
            app:backgroundTint="@color/green_600"
            app:cornerRadius="@dimen/dimen_5" />
    </LinearLayout>
</FrameLayout>
