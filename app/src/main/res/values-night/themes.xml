<resources>
    <!-- Dark window background prevents white flash before Compose renders. -->
    <style name="Theme.NewAkilimo" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/color_dark_background</item>
    </style>
</resources>
